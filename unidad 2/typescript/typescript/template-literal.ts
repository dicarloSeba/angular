(function(){
    function getEdad(){ 
        return 300 + 100 + 100;
    }
    const nombre: string = 'Seba';
    const apellido: string = 'Di Carlo';
    const edad: number = 34;
    
    const salida: string = `${ nombre }\n${ apellido }\n(${ edad})`;

    console.log(salida);

})();